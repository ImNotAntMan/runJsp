<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ page import="java.sql.*" %>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
<%
	request.setCharacterEncoding("utf-8");
	String b_subject = request.getParameter("b_subject");
	String b_contents = request.getParameter("b_contents");
	String b_name = request.getParameter("b_name");
	out.print(b_subject + " " + b_contents + " " + b_name);
	Connection conn = null;	// 접속 객체
	PreparedStatement psmt = null;	// 쿼리 객체
	ResultSet rs = null;	// 쿼리 결과 객체
	String query = "";	// sql 문법용 변수
	try {
		Class.forName("com.mysql.jdbc.Driver"); // JDBC 드라이버 로드
		String url = "jdbc:mysql://localhost:3306/mysql";	// 접속 url
		String user = "root";
		String passwd = "";
		conn = DriverManager.getConnection(url, user, passwd);
		out.print("드라이버 로드 성공<br>");
		out.print(conn + "<br>");
		query = "insert into tblboard(subject, contents, name) values(?, ?, ?)";
		psmt = conn.prepareStatement(query).executeQuery(query);	// 쿼리 객체 생성
//		rs = psmt.executeQuery();	// 쿼리 실행해서 결과를 rs에 반환받음
	} catch(Exception e) {
		out.print(e);	
	} finally {
		try {
			if(rs != null) 
				rs.close();
			if(psmt != null) 
				psmt.close();
			if(conn != null) 
				conn.close();
		} catch(Exception ex) {
			out.print(ex);
		}
	}

%>
</body>
</html>